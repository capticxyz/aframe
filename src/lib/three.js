var THREE = require('./three.module.js').default.THREE;
//var WebGPURenderer = require('./three.module.js').default.WebGPURenderer;

//var GPU = require('./three.module.js').GPU;
// In-memory caching for XHRs (for images, audio files, textures, etc.).
/*
if (THREE.Cache) {
  THREE.Cache.enabled = true;
}
*/
//console.error(WebGPURenderer)

//console.error(THREE)
module.exports = THREE;
